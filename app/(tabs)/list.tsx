import {View, Text} from "react-native";
import Counter from "@/components/counter";

const ListScreen = (() => {
    return (
        <View>
            <Text>GGG</Text>
            <Counter />
        </View>
    );
});

export default ListScreen